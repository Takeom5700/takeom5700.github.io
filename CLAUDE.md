# takeom5700.github.io

GitHub Pages で公開している個人サイト。公開元は **`main` ブランチのルート**。
`main` にマージされるまで公開ページは変わらない。

| パス | 中身 |
|---|---|
| `index.html` | トップページ |
| `uranai/` | 占いダッシュボード（毎日更新） |
| `ai-shinbun/` | AI新聞（GitHub Actions が毎日自動生成） |
| `mistlands/`, `column-*.html` | 静的ページ |

---

# 占いダッシュボード（uranai/）の更新ルール

**このセクションは必ず守ること。過去に日付が4つ混在する事故を起こしている。**

## 絶対にやってはいけないこと

- ❌ **日々の更新で `uranai/index.html` を直接編集しない。** 日付・日干支・
  六星の日運が本文の何十か所にも散っているため、手で書き換えると必ず
  更新漏れが出る。実際に「title は 9/5・本文は 9/2・六星は 9/4・
  フッターは 9/1」という事故が起きた。日々の更新は `daily.json` だけを触る。
  （UI や構造そのものを変えるときは `index.html` を編集してよい。
  その場合も必ず `build_uranai.py --check` を通してからコミットすること）
- ❌ **期限切れのチップを HTML に残さない。** 過去の日付のチップは
  ラッキーカラー欄から削除する。積み上げると表示が過去のもので埋まる。
- ❌ 日干支や六星の日運を自分で暗算しない。**必ずスクリプトに計算させる。**
- ❌ 検証が通らないままコミットしない。

## 正しい更新手順

```bash
# 1. 日付を進める（日干支・十神・六星の日運・月柱・天中殺は全部自動計算）
python3 .github/scripts/build_uranai.py --roll 2026-09-06

# 2. uranai/data/daily.json を開き、"TODO" と書かれた項目を全部埋める
#    ＝ その日の総合方針（summary.*）と スコア10項目のコメント。
#    "_review" に挙がった項目（Loveちゃん・しいたけ・山田ありす等）が
#    最新かどうかもここで確認し、古ければ差し替える。

# 3. HTML に差し込む（検証に落ちたら index.html は一切書き換わらない）
python3 .github/scripts/build_uranai.py

# 4. コミットして push
git add uranai/ && git commit -m "占いダッシュボード更新 2026-09-06"
```

`--roll` を使わず日付だけ手で変えるのは不可。日干支と六星がずれる。

## 仕組み

日付・日干支・六星の日運・スコアは **`uranai/data/daily.json` だけ** が持つ。
`uranai/index.html` は差し込み口しか持たない。

| 仕組み | 書き方 | 用途 |
|---|---|---|
| スロット | `<span data-uranai="KEY"></span>` | 文中に値を1つ差し込む |
| 生成ブロック | `<!-- uranai:begin NAME -->` … `<!-- uranai:end NAME -->` | 中身をまるごと生成 |

新しい項目を足したいときは、`daily.json` の `slots` にキーを追加し、
`index.html` にスロットを置く。**どちらか片方だけだと検証が落ちる**（V1）。

## 自動計算される値（触らなくてよい）

`--roll` が暦から計算する。手で書くと V0 検証で落ちる。

- 日付表示・週の範囲
- 日干支と十神（命式は **辛亥日主**）
- 翌日の日干支
- 六星占術の日運（木星人＋ / 金星人・霊合の2つ。12日周期）と翌日の日運
- 月柱（節入り表ベース。境界±1日は警告が出るので暦で確認する）
- 寅卯天中殺の該当/非該当

## 検証（`build_uranai.py`）

検証に1つでも落ちたら `index.html` を**一切書き換えず** exit 1 する。

| | 内容 |
|---|---|
| V0 | 日干支・翌日干支・月柱・六星日運が暦の計算値と一致するか |
| V1 | 全スロットが配置され、未知のスロットが無いか（更新漏れの検出） |
| V2 | 生成ブロックが揃っているか |
| V3 | `<meta name="data-date">` と `<title>` が対象日と一致するか |
| V4 | 「本日 / 現在 / 時点」と併記された日付が対象日以外に無いか |
| V5 | 対象日・翌日・命式以外の日干支が本文に残っていないか |
| V6 | 六星の運気名がスロット外にベタ書きされていないか |
| V7 | `daily.json` に埋め忘れ（TODO）が残っていないか |
| `--check` | 生成結果とコミット済み HTML が完全一致するか（部分更新の検出） |

`.github/workflows/uranai-validate.yml` が push / PR ごとに `--check` を走らせる。

## 古さの可視化

`<meta name="data-date">` を生成側が書き出し、ページ末尾のスクリプトが
Asia/Tokyo の今日と比較する。ズレていれば最上部に赤帯で
「⚠ データは○○時点（N日前）です」を強制表示する。
**更新が止まっても、古いデータが本日の運勢として読まれることはない。**
逆に言えば、赤帯が出ていたら更新が止まっているということ。

## 依頼者について

リポジトリの持ち主は技術的な詳細を追っていない。作業を報告するときは、
何をしたかより **「公開ページがどうなったか」** を平易な日本語で伝えること。
`main` にマージするまで公開ページは変わらない点は毎回明示する。

---

# ラッキーカラー欄（絞り込みと期限切れ）

チップは **種類**（🎨カラー / 🍜フード / 💎アイテム / 🔢数字 / 🧭方位 /
✨開運・運気 / ⚠注意 / 📌その他）と **ソース** で絞り込める。
分類はラベルの文言から自動判定するので、新しいチップを足すときは
ラベルに「ラッキーカラー」「ラッキーフード」「注意」などの語を入れておくと
正しい種類に入る。

期限切れのチップはページ末尾のスクリプトが自動で非表示にする。
ラベルの書き方で「いつまで有効か」が決まる。

| ラベルの書き方 | 有効期限 |
|---|---|
| `週8/31-9/6` `8/30-9/1`（月をまたぐ範囲） | 後ろの日付まで |
| `9/2-4` `（12星座・9/1-3）`（同じ月の範囲） | その月の後ろの日まで |
| `9月` `9月予言` `（8月）` | その月の末日まで |
| `8/15 推奨行動` `10/24`（単発の日付） | その日まで |
| 日付なし（年間・下半期・固定属性） | 期限なし・出し続ける |

**本日限りの値（六星の日運・その日の四柱推命/中国式）は
`daily.json` の `daily_chips` が生成する。**
同じ内容を手書きのチップとして置かないこと（重複して古い方が残る）。
